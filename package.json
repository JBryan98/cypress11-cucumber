{
  "name": "cypress-cucumber",
  "version": "1.0.0",
  "description": "Implementando cucumber a la version 11 de Cypress",
  "main": "index.js",
  "scripts": {
    "test": "cypress open"
  },
  "keywords": [
    "cypress",
    "javascript",
    "cucumber"
  ],
  "author": "Bryan Corrales",
  "license": "ISC",
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^14.0.0",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.1.5",
    "cypress": "^11.2.0",
    "esbuild": "^0.15.15"
  },
  "cypress-cucumber-preprocessor": {
    "stepDefinitions": [
      "cypress/e2e/steps-definitions/*.{js, ts}"
    ]
  }
}
